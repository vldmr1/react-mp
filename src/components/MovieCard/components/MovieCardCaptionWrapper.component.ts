import styled from 'styled-components';

const MovieCardCaptionWrapper = styled.div`
  display: flex;
  flex-direction: column;
  min-height: 6.5rem;
  margin-right: 1rem;
`;

export default MovieCardCaptionWrapper;
