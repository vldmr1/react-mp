export enum ToggleLabel {
  ReleaseDate = 'release date',
  Rating = 'rating',
  Title = 'title',
  Genre = 'genre',
}

export enum ToggleTitle {
  SortBy = 'sort by',
  SearchBy = 'search by',
}

export enum ToggleName {
  SortBy = 'sortBy',
  SearchBy = 'searchBy',
}
